---
- id: P0-2
  title: "Fix no-op deleteById await"
  file: app/backend.server/models/common.ts
  line: 53
  root_cause: >
    In deleteByIdForStringId (line 51), the existence check at line 53 reads
    `const existingRecord = tx.select({}).from(table).where(eq(table.id, id))`
    without await. This returns a Drizzle query-builder Promise object (always
    truthy), so the `if (!existingRecord)` guard on line 54 never triggers. The
    delete operation on line 56 proceeds regardless of whether the record exists,
    and the function silently succeeds for non-existent IDs. The function is
    imported and called by at least seven model files (organization, losses,
    disruption, damages, asset, noneco_losses, disaster_record__sectors).
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: >
    Add await before the tx.select call on line 53 so the existence check is
    actually evaluated before proceeding with the delete.
  antipattern: AP-07
---
