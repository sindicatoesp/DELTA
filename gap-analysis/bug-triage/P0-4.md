---
- id: P0-4
  title: "Add coverage thresholds baseline"
  file: vitest.config.ts
  line: 30
  root_cause: >
    The coverage block in vitest.config.ts (line 30) configures provider, include
    patterns, and reporters, but contains no thresholds property. Without a
    threshold, coverage can drop to 0% without any CI failure. The item notes
    require measuring the current passing coverage first and setting the threshold
    at that level to prevent regression from the existing baseline, not to enforce
    an aspirational target. Current baseline has not been committed to config.
  fix_complexity: Trivial
  requires_spec: false
  requires_test: false
  fix_summary: >
    Run `yarn coverage`, record the current lines/branches/functions/statements
    percentages, and add a thresholds block to the coverage config matching
    those values.
  antipattern: null
---
