---
- id: P0-8
  title: "Fix revokeUserApiAccess sets emailVerified=false"
  file: null
  line: null
  root_cause: >
    The function revokeUserApiAccess is referenced in the copilot-instructions as
    existing in app/backend.server/models/api_key.ts, but no such function exists
    in the current version of that file. The file currently contains only apiAuth()
    and two TypeScript type exports. The function was either removed, renamed, or
    never committed. Because the function is absent from the codebase, the bug
    cannot be reproduced or fixed against current source. The item should be
    verified against the git log to confirm whether the function was previously
    present and has been deleted, or whether it is still outstanding under a
    different name.
  fix_complexity: Trivial
  requires_spec: false
  requires_test: false
  fix_summary: >
    Verify via git log whether revokeUserApiAccess was previously present;
    if the bug is gone confirm closure, otherwise locate the function by its
    current name and fix the emailVerified side-effect.
  antipattern: AP-13
---
