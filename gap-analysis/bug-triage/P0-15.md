---
- id: P0-15
  title: "Fix destroyUserSession graceful handling of missing session"
  file: app/utils/session.ts
  line: 255
  root_cause: >
    destroyUserSession() in app/utils/session.ts checks
    `if (typeof sessionId === "string")` before executing the DB delete, and
    always calls sessionCookie().destroySession(session) regardless. The code
    includes the comment "Keep logout idempotent: missing/invalid sessionId
    should still clear cookie." If sessionId is absent (expired or already-
    destroyed session), the delete is skipped and the cookie is cleared — the
    function returns successfully. If sessionId is a valid string but the
    corresponding DB row has already been deleted, the Drizzle DELETE executes
    with 0 rows affected and returns without error. The function is idempotent
    in both cases. This item appears to be already resolved.
  fix_complexity: Trivial
  requires_spec: false
  requires_test: false
  fix_summary: >
    Verify in production that destroyUserSession does not throw during logout
    after session expiry; the current implementation appears already idempotent.
    Close the item if no regression is found.
  antipattern: null
---
