---
- id: P0-16
  title: "Delete dead cost calculation API endpoints (4 files, zero callers)"
  file: app/routes/$lang+/api+/disaster-events.$disaster_event_id+/replacement-cost.tsx
  line: 4
  root_cause: >
    Four route files expose unauthenticated API endpoints that have zero callers
    in the codebase: replacement-cost.tsx, repair-cost.tsx, rehabilitation-cost.tsx,
    and recovery-cost.ts under
    app/routes/$lang+/api+/disaster-events.$disaster_event_id+/.
    Each exports a loader with no auth wrapper (no authLoaderWithPerm call),
    meaning any unauthenticated request can query cost calculations for any
    disaster event ID. The underlying calculator functions
    (calculateTotalReplacementCost, calculateTotalRepairCost,
    calculateTotalRehabilitationCost, calculateTotalRecoveryCost) from
    disaster-events-cost-calculator are also called internally via
    updateTotalsUsingDisasterRecordId and are not dead — only these four HTTP
    route handlers are unreferenced. The routes are also missing tenant scoping.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: >
    Delete the four route files (replacement-cost.tsx, repair-cost.tsx,
    rehabilitation-cost.tsx, recovery-cost.ts) under
    app/routes/$lang+/api+/disaster-events.$disaster_event_id+/.
  antipattern: null
---
