---
- id: P0-3
  title: "Remove debug console.log + lint rule"
  file: app/init.server.tsx
  line: 7
  root_cause: >
    Unstructured console.log/warn/error calls are scattered across the codebase
    (274 calls across 55 files per the anti-pattern catalogue). Key locations
    include: app/init.server.tsx (lines 7-14, 5 console.log calls at server
    startup), app/entry.server.tsx (line 12), and
    app/routes/$lang+/setup+/admin-account-sso.tsx (line 9, logs NODE_ENV value
    to stdout). A structured logger already exists at app/utils/logger.ts.
    No ESLint rule prevents future additions. The item requires both removing
    existing debug logs and adding a lint rule to enforce the structured logger.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: >
    Replace console.log/warn/error calls with the structured logger from
    app/utils/logger.ts; add an ESLint no-console rule to prevent regression.
  antipattern: AP-04
---
