---
- id: P0-0
  title: "Remove/guard example & dev-example routes in production"
  file: app/routes/$lang+/api+/asset+/csv-import-example.ts
  line: 4
  root_cause: >
    Seven csv-import-example route files exist under
    app/routes/$lang+/api+/*/csv-import-example.ts (asset, damage, disaster-event,
    disaster-record, disruption, hazardous-event, losses). Each is wrapped with
    authLoaderWithPerm("EditData") via createExampleLoader in
    app/backend.server/handlers/form/csv_import.ts. These routes serve CSV
    template files for data import and are guarded by authentication. No
    unguarded "dev-example" routes were found in the codebase. The routes are
    reachable in production but require EditData permission. The item notes demand
    a NODE_ENV !== 'production' guard or deletion, but the routes serve a
    legitimate production purpose (CSV import templates). This creates a
    contradiction that requires a human decision on whether to add a redundant
    NODE_ENV guard or accept the auth guard as sufficient.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: >
    Confirm whether csv-import-example routes are intended for production use;
    if not, add NODE_ENV !== 'production' guard returning 404, or delete the
    7 route files.
  antipattern: null
---
