---
- id: P0-18
  title: "Add .dockerignore — prevent image bloat and secret leak"
  file: .dockerignore
  line: 1
  root_cause: >
    A .dockerignore file exists and already excludes .env files, private keys,
    node_modules, and build outputs — the secret-leak concern is addressed. However,
    the file does not exclude test directories (tests/, specs/, gap-analysis/),
    developer documentation (_docs/), Playwright config (playwright.config.ts),
    coverage output (coverage/), or the scripts/delta-deepl-translate/ tooling.
    Including these in the image adds bloat without runtime benefit. The item is
    partially addressed; the remaining work is to add the development-only
    directories and files to the exclusion list.
  fix_complexity: Trivial
  requires_spec: false
  requires_test: false
  fix_summary: >
    Add tests/, specs/, gap-analysis/, _docs/, coverage/, and
    playwright.config.ts to .dockerignore to prevent developer-only files from
    being copied into the production image.
  antipattern: null
---
